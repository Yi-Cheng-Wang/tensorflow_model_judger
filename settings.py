TOKEN_PREFIX = 'your_token_prefix' # Replace it with your own!
MAX_FILESIZE = 50 * 1024 * 1024 # default 50MB
TOKEN_DB = 'token_db/tokens.db' # default 'token_db/tokens.db'
SCORE_DB = 'score_db/scores.db' # default 'score_db/scores.db'